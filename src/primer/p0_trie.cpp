#include "primer/p0_trie.h"

// 这是一个用于 clang-tidy 检查的占位文件。
//
// 有了这个文件，我们可以运行 run_clang_tidy.py 来检查 `p0_trie.h`，
// 因为它会过滤掉所有头文件并且不会检查仅包含头文件的代码。
//
// 这个文件不是提交的一部分。所有的修改都应该在 `src/include/primer/p0_trie.h` 中完成。
